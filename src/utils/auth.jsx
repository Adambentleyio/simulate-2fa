
const correctlogin = '0000'

const authUser = (inputs) => {

    setTimeout(() => {
        if (inputs === correctlogin) {
        return true
        } else
        return false
      }, 1000)


      return

}

export default authUser